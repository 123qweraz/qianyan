import { CandidateData, CandidateWindow } from "candidate.slint";
import { KeystrokeWindow } from "keystroke.slint";

export { CandidateData, CandidateWindow, KeystrokeWindow }

export component AnchorWindow inherits Window {
    title: "QianyanIMEAnchor";
    no-frame: true;
    background: transparent;
    width: 1px;
    height: 1px;
    always-on-top: true;
}
