# Cargo deny configuration

[advisories]
# The path where the advisory database is cloned/fetched into
db-path = "~/.cargo/advisory-db"
# The url(s) of the advisory databases to use
db-urls = ["https://github.com/rustsec/advisory-db"]
# All vulnerability/unsound advisories automatically emit errors
# Scope for unmaintained advisories: all | workspace | transitive | none
unmaintained = "workspace"
# Scope for unsound advisories: all | workspace | transitive | none
unsound = "workspace"
# The lint level for crates that have been yanked from their source registry
yanked = "warn"
# The lint level for unused entries in the ignore list
unused-ignored-advisory = "warn"
# A list of advisory IDs to ignore. Note that ignored advisories will still
# output a note when they are encountered.
ignore = [
    # daemonize is unmaintained but no safe alternative for Linux daemonization
    "RUSTSEC-2025-0069",
]

[licenses]
# The lint level for unused entries in the allow list
unused-allowed-license = "warn"
# Licenses that are explicitly allowed. All others are denied by default.
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "ISC",
    "Unicode-DFS-2016",
    "Unicode-3.0",
    "CC0-1.0",
    "BSL-1.0",
    "Zlib",
    "Unlicense",
    "MPL-2.0",
    "NCSA",
    "LicenseRef-Slint-Royalty-free-2.0",
    "LicenseRef-Slint-Software-3.0",
]
# Note: GPL-2.0, GPL-3.0 are denied since they are not in the allow list
# The confidence threshold for detecting a license from license text
confidence-threshold = 0.8
# Ignore workspace crates' own license (handled at workspace level)
[licenses.private]
ignore = true

[bans]
# Lint level for when multiple versions of the same crate are detected
multiple-versions = "warn"
# Lint level for when a crate version requirement is `*`
wildcards = "allow"
# The graph highlighting used when creating dotgraphs for crates
highlight = "all"
# List of crates that are allowed. Use with care!
allow = []
# List of crates to deny
deny = []
# Certain crates/versions that will be skipped when doing duplicate detection
skip = []
# Similarly named crates that are allowed
skip-tree = []

[sources]
# Lint level for what to happen when a crate from a crate registry that is not
# in the allow list is encountered
unknown-registry = "warn"
# Lint level for what to happen when a crate from a git repository that is not
# in the allow list is encountered
unknown-git = "warn"
# List of URLs for allowed crate registries. Defaults to the crates.io index
# if not specified. If it is specified but empty, no registries are allowed.
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
# List of URLs for allowed Git repositories
allow-git = []